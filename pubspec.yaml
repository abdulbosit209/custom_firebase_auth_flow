name: custom_firebase_login
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.8.1

dependencies:
  authentication_repository:
    path: packages/authentication_repository
  equatable: ^2.0.7
  firebase_core: ^3.15.2
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  flutter_web_plugins:
    sdk: flutter
  font_awesome_flutter: ^10.8.0
  form_inputs:
    path: packages/form_inputs
  formz: ^0.8.0
  get_it: ^8.0.3
  google_fonts: ^6.2.1
  intl: ^0.20.2
  logging: ^1.3.0

dev_dependencies:
  flutter_lints: ^5.0.0
  flutter_test:
    sdk: flutter

flutter:
  generate: true

  uses-material-design: true
